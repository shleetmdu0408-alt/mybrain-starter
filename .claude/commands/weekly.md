---
description: 週次レビューを実行する（振り返り→反映→lint→Next Action→レポート）
---

`weekly-review` スキルを実行する。Step 1 から Step 7 まで省略せず最後まで行い、`reports/YYYY-MM-DD-週次レビュー.md` を必ず作成すること。

最後に `./.claude/hooks/lint-scan.sh --snapshot` を走らせて次回の基準を更新するのを忘れないこと。
