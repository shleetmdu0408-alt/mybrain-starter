---
description: Vault を同期する（デイリー・会話・raw の情報を wiki / MOC / index / Home に反映）
argument-hint: [同期したい範囲（省略可）]
---

`vault-sync` スキルを実行して、Vault の情報を同期する。

対象の指定: $ARGUMENTS （空なら直近のデイリーノート + 今の会話 + 未処理の `raw/` すべてを対象にする）
