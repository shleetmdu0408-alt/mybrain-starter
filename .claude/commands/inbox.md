---
description: スマホから inbox/ に届いたメモを読んで処理する（振り分け→実行→アーカイブ）
argument-hint: [特定のメモだけ処理したいときはファイル名（省略可）]
---

`inbox-intake` スキルを実行して、スマホから届いたメモを処理する。

対象の指定: $ARGUMENTS （空なら `inbox/` の未処理ファイルすべてが対象。`inbox/index.md` は対象外）
