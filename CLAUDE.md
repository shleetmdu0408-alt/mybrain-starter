# CLAUDE.md — この Vault の憲法（全体ルール）

> Claude Code が**毎セッション必ず読む**唯一のファイル。**既定の振る舞いを上書きする指示だけ**を置く。
> ここが膨らむほど毎回のコストが上がる。**増やす前に、既定やスキルで足りていないかを確認する。**
>
> 🔧 **セットアップ時にここを自分用に書き換える。** `<>` で囲った部分がその箇所（→ `docs/05_カスタマイズ.md`）

## オーナー

- **<氏名>**（<職種：例 整形外科医／内科医／薬剤師／研究者>）。詳細な事実は `Memory.md`
- **日本語で回答する**（固有名詞・コード・技術用語は原語のまま）
- プログラミング経験は<ほぼ無い／少しある>。コードを出すときは「何をするコードか」「どこに貼るか」「どう実行するか」**まで書く**
- **開発用語・IT用語は初出のその行で言い換える**（コードを出す場面に限らず、調査回答でも）。用語集を末尾にまとめない。ツール名は**初出で「誰が作った何をする道具か」を1行**添える

## 書き込み先（迷ったらここ）

- **記憶は Claude Code のメモリ領域ではなく、この Vault に置く**。Vault の外にオーナーの個人情報を書き出さない
- オーナーの**1年で変わらない事実** → `Memory.md`／**知識と変動する情報**（金額・日程・残タスク）→ `wiki/` の正本ページ（目次 `wiki/index.md`、テーマ地図 `wiki/moc/`）
- **同じ事実を2箇所に書かない（1トピック1正本）**。正本を1ページ決め、他のページからは `[[リンク]]` だけを張る。書く前に「これはどのページが正本か」を確認する
- **問いへの答え・調べもの** → `reports/` に .md で残し、根拠の `wiki/` ページへリンクする。チャットに流して消さない
  - **結論は `wiki/` の正本ページに統合**し、`reports/` には**経緯・出典・判断の根拠**だけを残す
  - **`reports/archive/` へ移すのは「同じ主題の新版が出て、旧版が読まれる理由が無くなったもの」だけ。「完了したから」では移さない**
- **修正指示** → `rules/corrections/<カテゴリ>.md`／**失敗** → `rules/mistakes/<カテゴリ>.md`／**命名規則** → `rules/naming.md`
- **散らばりやすい3つは置き場を固定する**（他のページに書かない。書きたくなったらリンクだけ張る）

  | 書きたいもの | 唯一の正本 |
  |---|---|
  | 答えの出ていない問い | `wiki/open_questions.md` |
  | 進行中の案件・期限 | `Home.md`「進行中」 |
  | Vault 自体の矛盾・欠落 | `wiki/lint-issues.md` |
- **オーナーに渡す成果物**（PDF/docx/xlsx/pptx）→ `outputs/deliverables/` に `YYYY-MM-DD_主題.拡張子`。`.md` では渡さない（手順は `deliverable` スキル）
- `raw/` は**読み取り専用**の素材置き場。編集・改名・削除しない（書き込みは `.claude/settings.json` で禁止済み）
- `inbox/` は**スマホから届いた未処理メモ**。**読むだけ・最後に `done/` へ移すだけで、中身を書き換えない**（スマホ側と衝突して同期が競合コピーを作る）。処理は `inbox-intake` スキル
- **整理しすぎない**。フォルダで細かく分類しない。分類より `[[リンク]]` と命名で繋ぐ
  - **減らしてよいのは「重複」と「語り」だけ。索引・1行要約・ルーティング表・節索引は減らさない**（これがあるから部分読みができる。消すと逆に読む量が増える）

- **状態を書くときは事実だけ。AI の評価語を書かない**
  - 禁止：`停滞中` `未着手` `遅れている` `3週間記録なし` — **記録が無いことは、やっていないことの証拠にならない**（オーナーは Vault の外で動いている）
  - 書いてよいのは：`最終更新 2026-01-15` のような**事実**、オーナーが明言した状態（`保留`／`待機`／`3/1 返送予定`）
  - **`待機` は「オーナーが対応中。催促しない」の意味**。この状態のものを Next Action や週次レビューに出さない
  - 進捗が知りたいときは Vault に推測を書かず、**オーナーに「これはどうなりましたか？」と聞く**

## 読み込みルール（毎回全部読まない）

- 常に読むのはこの `CLAUDE.md` だけ
- `Memory.md` は「変わらない事実」だけの短いファイル。**オーナー本人や家族の話題なら全文読んでよい**。雑談・一般的な質問では読まない。金額・日程・進捗を聞かれたら Memory ではなく **`wiki/index.md` が示す正本ページ**を読む
- `wiki/` は `wiki/index.md` を目次にして必要なページだけ開く。探すときは `Grep`/`Glob`（Read で総なめしない）
- **Vault 全体を検索するときは `raw/ reports/archive/ rules/archive/` を除外する**（素材と保管分。ノイズになる）
- **読む前に量を見積もる。** 開く URL・PDF が**3件以上**、または合計**1万字を超えそう**なら、読み始める前に外部ツールへの外注（`/nlm`）を提案する。`WebSearch` で集めた URL が3本以上になったときも同じ（**読んでから重いと気づいても遅い**）

| テーマ | 追加で見る場所 |
|---|---|
| 研究・論文 | `wiki/research_*` |
| 臨床の知識 | `wiki/clinical_*` |
| 仕事・キャリア | `wiki/career_*` |
| 家族・生活 | `wiki/life_*` |
| ツール・ワークフロー | `wiki/tools_*` |

- **この Vault の使い方・仕組みを聞かれたら `docs/` を読む**（初心者向けの説明の正本。道具の一覧は `wiki/tools_and_workflow.md`）
- 「〇〇の続き」と言われたら：`wiki/` `reports/` `daily/` を Grep → **前回の文脈を1行で要約してから**着手する
- **大きい wiki ページは冒頭の「節索引」に従い、該当する節だけ Read する**（全文を読まない）
- **ルールは着手前に該当1ファイルだけ読む**（`rules/corrections.md` `rules/mistakes.md` はルーティング表なので普段は開かない）
  - 医学・研究・論文 → `rules/corrections/medical.md`／成果物・長文 → `rules/corrections/deliverable.md` ＋ `rules/design/<形式>.md`
  - 金額・数値・体験 → `rules/mistakes/facts.md`／一次資料 → `rules/corrections/facts.md`／手順書 → `rules/mistakes/procedures.md`
  - 案件の状態 → `rules/mistakes/status.md`／Vault の仕組み → `rules/corrections/vault.md`／ファイル名・frontmatter → `rules/naming.md`

## 絶対に守る運用ルール

- **修正の資産化**：訂正・要望を言われたら、**その場で** `rules/corrections/<該当カテゴリ>.md` に3行（日付／指摘／今後どう動くか）追記する。同じことを二度言わせない。同じ失敗を指摘されたら `rules/mistakes/<該当カテゴリ>.md` に再発防止ルールを書く。**追記の前に、既存の項目に統合できないかを見る**
- **素材の構造化**：`raw/` に素材が来たら、**まず「何のために入れたか」をオーナーに聞いてから動く**（同じ素材でも、構造化・要約・調査の出発点・単なる保管で、やることが変わる）。意図が言われている場合だけそのまま進める
- **Memory と Home を最新に**：前提・進行中・判断基準が変わったら `Memory.md` を更新。重要ノート・MOC・進行中の案件が増えたら `Home.md` にリンクを1行足す
- **Obsidian 構文で書く**：内部リンクは `[[wikilink]]`、メタは frontmatter（type/status/date/topic/tags）、強調は callout（`> [!note]`）
- **医学的な質問は一次文献に当たる**：臨床・研究の質問は記憶や一般 WebSearch で答えず、**まず論文を引く**（`paper-search` スキル＝`/paper`）。**PMID と DOI を必ず併記**する
- **時事性のあることは調べる**：制度・価格・ガイドライン・論文の最新動向は、必ず `WebSearch` で確認してから答える。記憶で答えない。**数値は公式サイトか独立した2ソース以上で一致を確認**してから書く（1ソースしか根拠が無ければ「未確認」と明記）
- **日付は確認する**：`date +%Y-%m-%d` で取る。推測で日付を書かない
- **患者情報を書かない**：症例を扱うときも、氏名・ID・生年月日・施設名は Vault に書かない。**外部サービス（NotebookLM 等）には匿名化済みでも出さない**
- **スキルを使う**：該当する作業では `.claude/skills/` のスキルを必ず使う。週に一度 `weekly-review`（`/weekly`）を回す

## 文体・判断基準

- **結論・要旨を先に、詳細を後に**
- わかりやすい言葉と論理的な構造。誤りがあれば明確に否定する
- 選択肢を出すときは推奨案を明示しつつ、デメリットも書く
- 迷ったら**スピードより質**。不確実なことは推測で語らず、ファクトチェックする
