# SETUP — インストールから最初の1回まで

はじめて Claude Code を使う人向けに、**上から順にやれば動く**ように書いています。全部で15分ほどです。
分からない用語が出てきたら [docs/04_用語ミニ辞典.md](docs/04_用語ミニ辞典.md) を見てください。

---

## 0. 用意するもの

| 要るもの | 補足 |
|---|---|
| Mac または Windows のパソコン | このテンプレートは **Mac で作って動かしています**。Windows でも動きますが、一部のコマンド（`date -v` など）は書き換えが要ります |
| Claude の有料プラン（Pro / Max） | Claude Code を使うのに必要です |
| （任意）Obsidian | ノートを人の目で読み書きするアプリ。無くても Claude Code だけで動きます |

---

## 1. Claude Code を入れる

**いちばん簡単なのはデスクトップアプリです。**

1. <https://claude.com/claude-code> を開く
2. 自分の OS 向けのアプリをダウンロードしてインストールする
3. 起動して、Claude のアカウントでログインする

> ターミナル（黒い画面）で使いたい人は、`npm install -g @anthropic-ai/claude-code` でも入ります。
> **初心者はデスクトップアプリで大丈夫です。** 以下の手順はどちらでも同じです。

---

## 2. このテンプレートをダウンロードする

### 方法A：ZIP でダウンロード（簡単）

1. GitHub のページ右上の緑色の **「Code」** ボタン → **「Download ZIP」**
2. ダウンロードした ZIP を解凍する
3. できたフォルダの名前を、自分の好きな名前に変える（例：`MyBrain`）

### 方法B：git を使う（慣れている人向け）

```bash
git clone https://github.com/shleetmdu0408-alt/mybrain-starter.git MyBrain
```

---

## 3. フォルダの置き場所を決める

**スマホからもメモを入れたい**なら、クラウド同期されるフォルダの中に置きます。

| 使うもの | 置き場所の例 |
|---|---|
| Google ドライブ | `~/Library/CloudStorage/GoogleDrive-<自分のアドレス>/マイドライブ/MyBrain` |
| iCloud | `~/Library/Mobile Documents/com~apple~CloudDocs/MyBrain` |
| 同期しない（パソコンだけ） | `~/Documents/MyBrain` |

> **注意：** クラウド同期を使うと、同期のたびにファイルの更新日時が書き換わります。
> このテンプレートはそれを前提に作ってあるので（日付は frontmatter で管理）、そのままで大丈夫です。

---

## 4. Claude Code でこのフォルダを開く

- **デスクトップアプリ**：「フォルダを開く」から、いま置いた `MyBrain` フォルダを選ぶ
- **ターミナル**：そのフォルダに移動して `claude` と打つ

  ```bash
  cd ~/Documents/MyBrain
  claude
  ```

開いた瞬間に `CLAUDE.md`（この Vault のルール）が読み込まれます。

---

## 5. 動くか確かめる（3つ試す）

### ① 自己紹介させてみる

```
この Vault の使い方を、docs/ を読んで3行で教えて
```

日本語で答えが返ってくれば成功です。

### ② 朝のブリーフィングを動かす

```
おはよう
```

天気・記念日・進行中の案件などが出れば、**フック（自動で動く仕組み）が効いています**。
（天気が出ない場合は、ネットに繋がっているか確認してください）

### ③ 論文検索を動かす

```
/paper 変形性膝関節症に運動療法は有効か
```

PubMed の検索が走れば成功です。**うまく動かない場合は [docs/02_PubMedの使い方.md](docs/02_PubMedの使い方.md) を見てください。**

> 途中で「このコマンドを実行してよいですか？」と聞かれたら **許可してください**。
> Claude Code は、ファイルを書き換えるときや外部にアクセスするとき、毎回確認を取ります。

---

## 6. 自分用に書き換える（ここが本番）

最初のセッションで、次の3つだけ埋めます。**AI に手伝わせてください。**

```
Memory.md を私の情報に書き換えたい。質問してください
```

| ファイル | 何を書くか |
|---|---|
| `Memory.md` | 名前・職業・専門・家族構成・AI への要望。**1年で変わらない事実だけ** |
| `CLAUDE.md` の「オーナー」節 | 名前・職種・プログラミング経験の有無 |
| `.claude/hooks/morning-brief.sh` の `LAT` / `LON` / `PLACE` | 天気を出す地点（既定は東京駅） |

そのほか書き換える場所の一覧は [docs/05_カスタマイズ.md](docs/05_カスタマイズ.md) にまとめてあります。

---

## 7. （任意）Obsidian を入れる

Claude Code だけでも動きますが、**自分の目でノートを読む**なら Obsidian が便利です。

1. <https://obsidian.md> からダウンロード
2. 「Open folder as vault」で `MyBrain` フォルダを選ぶ
3. `[[リンク]]` がクリックできるようになり、ノート同士の繋がりがグラフで見えます

スマホの Obsidian も入れておくと、`inbox/` にメモを投げられます（→ `inbox/index.md`）。

---

## 8. 【重要】プライバシーの設定

このフォルダは、使い始めると**自分の記録そのもの**になります。

### git を使わない場合（推奨・簡単）

方法A（ZIP）でダウンロードしたなら、何もしなくて大丈夫です。

### git を使う場合

方法B（`git clone`）でダウンロードした人は、**元のリポジトリと繋がったまま**になっています。次のどちらかにしてください。

**(a) git を切り離す（いちばん安全）**

```bash
cd ~/Documents/MyBrain
rm -rf .git
```

**(b) 自分の private リポジトリにする**

GitHub で **Private** の新しいリポジトリを作り、そこに向け直します。

```bash
cd ~/Documents/MyBrain
git remote set-url origin https://github.com/<自分のアカウント>/<新しいリポジトリ名>.git
```

> **絶対にやってはいけないこと：** 自分の記録が入った状態で **public リポジトリに push すること**。
> 患者情報・個人情報が世界中から見えるようになります。

---

## つまずいたときは

| 症状 | 対処 |
|---|---|
| `おはよう` で何も出ない | フックは `.claude/settings.json` で設定されています。Claude Code を再起動してみてください |
| 「権限がありません」と出る | 確認を求められたら許可してください。毎回聞かれるのが煩わしければ、AI に「この操作をいつも許可したい」と伝えてください |
| コマンド（`/paper` など）が出てこない | フォルダを正しく開けていない可能性があります。`CLAUDE.md` があるフォルダを開いてください |
| 動きがおかしい | **AI 本人に聞いてください。** 「`.claude/` の中を見て、なぜ動かないか調べて」と頼めば自分で調べます |
