# AGENTS.md — Claude Code 以外のエージェント用の起点

> この Vault のメインエージェントは **Claude Code**。
> **ルールの正本は [`CLAUDE.md`](CLAUDE.md) 1ファイル。まずそれを読み、その内容に従うこと。**
> このファイルは `CLAUDE.md` を読めない場合の最小限のバックアップで、**ルーティング表などを二重に持たない**（更新漏れの元になるため）。

## CLAUDE.md が読めないときの最低限

1. **日本語で回答する**（固有名詞・技術用語は原語のまま）
2. **同じ事実を2箇所に書かない**。1年で変わらない事実は `Memory.md`、変動する情報は `wiki/` の正本ページ、恒久ルールは `rules/corrections/<カテゴリ>.md`
3. `raw/` は**読み取り専用**（削除・改名・編集しない）。`inbox/` は読んで `done/` へ移すだけ
4. 答えは `reports/` にファイルで残し、`wiki/` ページへ `[[リンク]]` する
5. 成果物（PDF/docx/xlsx/pptx）は `outputs/deliverables/` に `YYYY-MM-DD_主題.拡張子` で保存。`.md` では渡さない
6. 削除・上書き・大量改名・外部送信の前は必ず確認する
7. 日付・数値・固有名詞は確証がなければ「未確認」と明示する。医学的な主張は一次文献（PMID/DOI）を付ける
8. **患者情報を書かない。** 外部サービスへ出さない
9. **全部読まない。** 入口は `CLAUDE.md` →（オーナー本人の話なら）`Memory.md` → `wiki/index.md` の目次から必要なページだけ

Claude Code 用のスキル定義は `.claude/skills/` にある。他のエージェントでも手順書として読んでよい。
